/**
 * Re-export shared corporate PDF-service payload mapper (CRM PDF editor only).
 * Uses the lightweight subpath — not the full kyc-providers entry (Digio, react-pdf).
 */
export * from "@root/kyc-providers/corporate-pdf-payload";
