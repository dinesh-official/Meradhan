
name: backend deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        

      - name: Create .env from GitHub Secrets
        shell: bash
        run: |
        
          # Static Public values
          printf "NEXT_PUBLIC_HOST_URL=https://www.meradhan.co" > .env
          printf "NEXT_PUBLIC_CRM_HOST_URL=https://crm.meradhan.co" >> .env
          printf "NEXT_PUBLIC_BACKEND_HOST_URL=https://api.meradhan.co" >> .env
          printf "NEXT_PUBLIC_BACKEND_IP_URL=http://prod-meradhan-be.prod-namespace:4000" >> .env
          printf "NEXT_PUBLIC_STRAPI_HOST_URL=https://spyder.meradhan.co" >> .env
          printf "NEXT_PUBLIC_DIGIO=production" >> .env
          printf "STRAPI_API_URL=https://spyder.meradhan.co/api" >> .env
          printf "PORT=4000" >> .env

          printf "RFQ_CBRICS_ENV=PROD" >> .env
          printf "CBRICS_ENV=PROD" >> .env

          # Secrets
          NEXT_PUBLIC_GRAPHQL_KEY="${{ secrets.NEXT_PUBLIC_GRAPHQL_KEY }}"
          printf "GRAPHQL_KEY=$NEXT_PUBLIC_GRAPHQL_KEY" >> .env

          STRAPI_API_TOKEN="${{ secrets.STRAPI_API_TOKEN }}"
          printf "STRAPI_API_TOKEN=$STRAPI_API_TOKEN" >> .env

          JWT_SECRET="${{ secrets.JWT_SECRET }}"
          printf "JWT_SECRET=$JWT_SECRET" >> .env

          DATABASE_URL="${{ secrets.DATABASE_URL }}"
          printf "DATABASE_URL=$DATABASE_URL" >> .env

          DIRECT_URL="${{ secrets.DIRECT_URL }}"
          printf "DIRECT_URL=$DIRECT_URL" >> .env

          REDIS_HOST="${{ secrets.REDIS_HOST }}"
          printf "REDIS_HOST=$REDIS_HOST" >> .env

          REDIS_PORT="${{ secrets.REDIS_PORT }}"
          printf "REDIS_PORT=$REDIS_PORT" >> .env

          KRA_USERNAME="${{ secrets.KRA_USERNAME }}"
          printf "KRA_USERNAME=$KRA_USERNAME" >> .env

          KRA_PASSWORD="${{ secrets.KRA_PASSWORD }}"
          printf "KRA_PASSWORD=$KRA_PASSWORD" >> .env

          KRA_PASS_KEY="${{ secrets.KRA_PASS_KEY }}"
          printf "KRA_PASS_KEY=$KRA_PASS_KEY" >> .env

          KRA_MOB_NO="${{ secrets.KRA_MOB_NO }}"
          printf "KRA_MOB_NO=$KRA_MOB_NO" >> .env

          KRA_OKRA_CD_MI_ID="${{ secrets.KRA_OKRA_CD_MI_ID }}"
          printf "KRA_OKRA_CD_MI_ID=$KRA_OKRA_CD_MI_ID" >> .env

          KRA_ENV="${{ secrets.KRA_ENV }}"
          printf "KRA_ENV=$KRA_ENV" >> .env

          MSG91_AUTH_KEY="${{ secrets.MSG91_AUTH_KEY }}"
          printf "MSG91_AUTH_KEY=$MSG91_AUTH_KEY" >> .env

          MSG91_SIGNUP_TEMPLATE="${{ secrets.MSG91_SIGNUP_TEMPLATE }}"
          printf "MSG91_SIGNUP_TEMPLATE=$MSG91_SIGNUP_TEMPLATE" >> .env

          MSG91_LOGIN_TEMPLATE="${{ secrets.MSG91_LOGIN_TEMPLATE }}"
          printf "MSG91_LOGIN_TEMPLATE=$MSG91_LOGIN_TEMPLATE" >> .env

          MSG91_VERIFY_TEMPLATE="${{ secrets.MSG91_VERIFY_TEMPLATE }}"
          printf "MSG91_VERIFY_TEMPLATE=$MSG91_VERIFY_TEMPLATE" >> .env

          SMTP_SENDER="${{ secrets.SMTP_SENDER }}"
          printf "SMTP_SENDER=\"$SMTP_SENDER\"" >> .env

          SMTP_HOST="${{ secrets.SMTP_HOST }}"
          printf "SMTP_HOST=$SMTP_HOST" >> .env

          SMTP_PORT="${{ secrets.SMTP_PORT }}"
          printf "SMTP_PORT=$SMTP_PORT" >> .env

          SMTP_USER="${{ secrets.SMTP_USER }}"
          printf "SMTP_USER=$SMTP_USER" >> .env

          SMTP_PASS="${{ secrets.SMTP_PASS }}"
          printf "SMTP_PASS=$SMTP_PASS" >> .env

          CBRICS_DOMAIN="${{ secrets.CBRICS_DOMAIN }}"
          printf "CBRICS_DOMAIN=$CBRICS_DOMAIN" >> .env

          CBRICS_LOGIN="${{ secrets.CBRICS_LOGIN }}"
          printf "CBRICS_LOGIN=$CBRICS_LOGIN" >> .env

          CBRICS_PASSWORD="${{ secrets.CBRICS_PASSWORD }}"
          printf "CBRICS_PASSWORD=$CBRICS_PASSWORD" >> .env

          RFQ_CBRICS_DOMAIN="${{ secrets.RFQ_CBRICS_DOMAIN }}"
          printf "RFQ_CBRICS_DOMAIN=$RFQ_CBRICS_DOMAIN" >> .env

          RFQ_CBRICS_LOGIN="${{ secrets.RFQ_CBRICS_LOGIN }}"
          printf "RFQ_CBRICS_LOGIN=$RFQ_CBRICS_LOGIN" >> .env

          RFQ_CBRICS_PASSWORD="${{ secrets.RFQ_CBRICS_PASSWORD }}"
          printf "RFQ_CBRICS_PASSWORD=$RFQ_CBRICS_PASSWORD" >> .env

          DIGIO_USERNAME_PASS="${{ secrets.DIGIO_USERNAME_PASS }}"
          printf "DIGIO_USERNAME_PASS=$DIGIO_USERNAME_PASS" >> .env

          NDSL_REQUESTOR_ID="${{ secrets.NDSL_REQUESTOR_ID }}"
          printf "NDSL_REQUESTOR_ID=$NDSL_REQUESTOR_ID" >> .env

          NSDL_SECRET_KEY="${{ secrets.NSDL_SECRET_KEY }}"
          printf "NSDL_SECRET_KEY=$NSDL_SECRET_KEY" >> .env

          CDSL_AES_KEY="${{ secrets.CDSL_AES_KEY }}"
          printf "CDSL_AES_KEY=$CDSL_AES_KEY" >> .env

          ENTITY_ID="${{ secrets.ENTITY_ID }}"
          printf "ENTITY_ID=$ENTITY_ID" >> .env

          AUTH_SECRET="${{ secrets.AUTH_SECRET }}"
          printf "AUTH_SECRET=$AUTH_SECRET" >> .env

          AUTH_GOOGLE_ID="${{ secrets.AUTH_GOOGLE_ID }}"
          printf "AUTH_GOOGLE_ID=$AUTH_GOOGLE_ID" >> .env

          AUTH_GOOGLE_SECRET="${{ secrets.AUTH_GOOGLE_SECRET }}"
          printf "AUTH_GOOGLE_SECRET=$AUTH_GOOGLE_SECRET" >> .env

          AUTH_FACEBOOK_ID="${{ secrets.AUTH_FACEBOOK_ID }}"
          printf "AUTH_FACEBOOK_ID=$AUTH_FACEBOOK_ID" >> .env

          AUTH_FACEBOOK_SECRET="${{ secrets.AUTH_FACEBOOK_SECRET }}"
          printf "AUTH_FACEBOOK_SECRET=$AUTH_FACEBOOK_SECRET" >> .env

          AUTH_MICROSOFT_ENTRA_ID_ID="${{ secrets.AUTH_MICROSOFT_ENTRA_ID_ID }}"
          printf "AUTH_MICROSOFT_ENTRA_ID_ID=$AUTH_MICROSOFT_ENTRA_ID_ID" >> .env

          AUTH_MICROSOFT_ENTRA_ID_SECRET="${{ secrets.AUTH_MICROSOFT_ENTRA_ID_SECRET }}"
          printf "AUTH_MICROSOFT_ENTRA_ID_SECRET=$AUTH_MICROSOFT_ENTRA_ID_SECRET" >> .env

          AUTH_MICROSOFT_ENTRA_ID_ISSUER="${{ secrets.AUTH_MICROSOFT_ENTRA_ID_ISSUER }}"
          printf "AUTH_MICROSOFT_ENTRA_ID_ISSUER=$AUTH_MICROSOFT_ENTRA_ID_ISSUER" >> .env

          RAZORPAY_KEY_ID="${{ secrets.RAZORPAY_KEY_ID }}"
          printf "RAZORPAY_KEY_ID=$RAZORPAY_KEY_ID" >> .env

          RAZORPAY_KEY_SECRET="${{ secrets.RAZORPAY_KEY_SECRET }}"
          printf "RAZORPAY_KEY_SECRET=$RAZORPAY_KEY_SECRET" >> .env

          RAZORPAY_WEBHOOK_SECRET="${{ secrets.RAZORPAY_WEBHOOK_SECRET }}"
          printf "RAZORPAY_WEBHOOK_SECRET=$RAZORPAY_WEBHOOK_SECRET" >> .env

          CDSL_MODE="${{ secrets.CDSL_MODE }}"
          printf "CDSL_MODE=$CDSL_MODE" >> .env

          NSDL_MODE="${{ secrets.NSDL_MODE }}"
          printf "NSDL_MODE=$NSDL_MODE" >> .env

          S3_ACCESS_KEY_ID="${{ secrets.S3_ACCESS_KEY_ID }}"
          printf "S3_ACCESS_KEY_ID=$S3_ACCESS_KEY_ID" >> .env

          S3_SECRET_ACCESS_KEY="${{ secrets.S3_SECRET_ACCESS_KEY }}"
          printf "S3_SECRET_ACCESS_KEY=$S3_SECRET_ACCESS_KEY" >> .env

          S3_REGION="${{ secrets.S3_REGION }}"
          printf "S3_REGION=$S3_REGION" >> .env

          S3_ENDPOINT="${{ secrets.S3_ENDPOINT }}"
          printf "S3_ENDPOINT=$S3_ENDPOINT" >> .env

          S3_BUCKET_NAME="${{ secrets.S3_BUCKET_NAME }}"
          printf "S3_BUCKET_NAME=$S3_BUCKET_NAME" >> .env

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Show .env
        run: cat .env

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, Tag & Push Docker Image
        env:
          REGISTRY: ${{ secrets.ECR_REGISTRY }}
          REPOSITORY: meradhan-backend
          IMAGE_TAG: latest
        run: |
          docker build -f backenddocker -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .
          docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG

      - name: Deploy to Amazon ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: meradhan-be-task.json
          cluster: prod-cluster
          service: prod-meredha-be
          force-new-deployment: true
