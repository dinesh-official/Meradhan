model CrmAuditLogs {
    id        Int      @id @default(autoincrement())
    userId    Int?
    type      String
    token     String?
    url       String?
    data      Json
    createdAt DateTime @default(now())

    @@map("crm_audit_logs")
}
